server:
  port: 9100

eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    serviceUrl:
      defaultZone: http://127.0.0.1:9001/eureka/

spring:
  application:
    name: study-server #服务名称
